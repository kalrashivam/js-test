import React from "react"
import Layout from "../components/layout.js"

export default () => (
    <Layout>
        <div>
            This is the created by Shivam Kalra if the page dosen't load properly,
            Add the bootstrap cdn to the index.html in the pulic static pages.
        </div>
    </Layout>
)